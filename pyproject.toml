[project]
name = "stock-picker"
version = "1.0.0"
description = "Daily stock picker using the 150-day moving average"
requires-python = ">=3.10"
dependencies = [
    "yfinance>=0.2.40",
    "pandas>=2.0.0",
    "streamlit>=1.32.0",
]

[project.scripts]
stock-picker = "stock_picker.cli:main"
